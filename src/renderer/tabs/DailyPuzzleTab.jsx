import DailyPuzzle from '../sessions/DailyPuzzle.jsx';

function DailyPuzzleTab({ settings, matches }) {
  return <DailyPuzzle settings={settings} matches={matches} />;
}

export default DailyPuzzleTab;
